%h1 歌ログ
= image_tag("icon/home.png", :size => "24x24")
= link_to("トップページに戻る", root_path)
%br
#sing_log_table
  - if @sing_logs.empty?
    Empty
  - else
    %ol
      - @sing_logs.each do |s|
        %li.queue
          %span.clearfix{:style => "float: right"}
            - if sing_log.user
              = sing_log.user.name + " | "
            = sing_log.created_at.strftime("%Y/%m/%d %H:%M")
          %div{:style => "display: inline-block; max-width: 550px;"}
            = image_tag("icon/music_48x48.png", :size => "24x24", :class=> "entity_icon")
            = link_to_if(true, sing_log.pasokara_files.name, queue_pasokara_path(sing_log.pasokara_file))

    = paginate @sing_logs
