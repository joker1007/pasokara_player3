%ul#menu
  %li= link_to "ホーム", root_path, :id => "home"
  %li= link_to "予約確認", queues_path, :id => "queue"
  %li= link_to "りれき", history_index_path, :id => "history"
  - if user_signed_in?
    %li= link_to("お気に入り", favorite_pasokaras_path, :id => "favorite")
    %li= link_to("設定変更", edit_user_registration_path, :id => "user_edit")
  %li= link_to("使い方", help_usage_path, :id => "usage")
