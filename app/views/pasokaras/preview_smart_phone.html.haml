:javascript
  function add_video_tag(id, path) {
    var video = $("<video>").attr("id", "video-" + id).attr("width", 480).attr("height", 320).attr("controls", "controls").attr("src", path);
    $("#video").append(video);
  }

  $(document).ready(function() {
    indicator_init();
    check_video("#{@pasokara.id}", "#{@movie_path}", add_video_tag);
  });

%h1= h @pasokara.name
%div#indicator{:style => "padding-left: 220px;"}
#video

